export default function Rules() {
	return (
		<>
			<div className="mt-6" />

			<div>Coming later...</div>
			{/* Actually, we'll make that a popup so the player doesn't lose its current game */}

			<div className="flex-1" />
		</>
	);
}
